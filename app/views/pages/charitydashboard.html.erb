<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Charity Dashboard | 1Cause</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f4f7fa;
            color: #222;
            text-align: center;
            padding-bottom: 80px; /* Add padding to prevent content from being hidden behind the footer */
        }

        /* Navbar */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 50px;
            background: #0077B6;
            color: white;
        }

        .navbar .logo {
            font-size: 28px;
            font-weight: bold;
        }

        .navbar .login-btn {
            background: white;
            color: #0077B6;
            padding: 10px 20px;
            border-radius: 20px;
            text-decoration: none;
            font-weight: bold;
            transition: 0.3s ease-in-out;
        }

        .navbar .login-btn:hover {
            background: #005F99;
            color: white;
        }

        /* Dashboard Header */
        .dashboard-header {
            padding: 50px 20px;
            background: linear-gradient(rgba(0, 158, 219, 0.8), rgba(0, 58, 100, 0.9)), url('https://source.unsplash.com/1600x900/?charity,helping');
            background-size: cover;
            background-position: center;
            color: white;
        }

        .dashboard-header h1 {
            font-size: 50px;
            font-weight: 700;
            margin-bottom: 20px;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.2);
        }

        /* Open Proposals Section */
        .proposals-section {
            padding: 40px 20px;
            max-width: 1000px;
            margin: auto;
        }

        .proposals-section h2 {
            font-size: 36px;
            margin-bottom: 20px;
            color: #0077B6;
            text-align: left;
        }

        .proposals-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
        }

        .proposals-table th,
        .proposals-table td {
            padding: 15px;
            border: 1px solid #ddd;
            text-align: left;
        }

        .proposals-table th {
            background-color: #0077B6;
            color: white;
            font-weight: bold;
        }

        .proposals-table tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .proposals-table tr:hover {
            background-color: #f1f1f1;
        }

        .amount-allocated {
            color: #0077B6; /* Blue for amount allocated */
            font-weight: bold;
        }

        /* New Proposal Button */
        .new-proposal-btn {
            background: #005F99;
            color: white;
            padding: 12px 25px;
            font-size: 18px;
            font-weight: bold;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            text-decoration: none;
            display: inline-block;
            margin-top: 20px;
        }

        .new-proposal-btn:hover {
            background: #004377;
        }

        /* AI Allocation Engine Button */
    .ai-allocation-btn {
        background: #005F99; /* Use a different color to distinguish it */
        color: white;
        padding: 12px 25px;
        font-size: 18px;
        font-weight: bold;
        border: none;
        border-radius: 50px;
        cursor: pointer;
        transition: all 0.3s ease-in-out;
        text-decoration: none;
        display: inline-block;
        margin-left: 20px; /* Add space between buttons */
    }

    .ai-allocation-btn:hover {
        background: #004377; /* Darker shade on hover */
    }

    </style>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
</head>
<body>

    <!-- Navbar -->
    <div class="navbar">
        <div class="logo">1Cause</div>
        <a href="#" class="login-btn">Login/Sign Up</a>
    </div>

    <!-- Dashboard Header -->
    <section class="dashboard-header">
        <h1>Charity Dashboard</h1>
    </section>

    <!-- Open Proposals Section -->
    <section class="proposals-section">
        <h2>Open Proposals</h2>
        <table class="proposals-table">
            <thead>
                <tr>
                    <th>Proposal Title</th>
                    <th>Requested Funds</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Biodiversity and Climate Change - Marine Ecosystems</td>
                    <td class="amount-allocated">$18,000</td>
                </tr>
                <tr>
                    <td>Biodiversity and Climate Change - Endangered Species</td>
                    <td class="amount-allocated">$25,000</td>
                </tr>
                <tr>
                    <td>Biodiversity and Climate Change - Reforestation</td>
                    <td class="amount-allocated">$12,000</td>
                </tr>
                <tr>
                    <td>Biodiversity and Climate Change - Education & Advocacy</td>
                    <td class="amount-allocated">$15,000</td>
                </tr>
                <tr>
                    <td>Biodiversity and Climate Change - Sustainable Agriculture</td>
                    <td class="amount-allocated">$11,000</td>
                </tr>
            </tbody>
        </table>

        <!-- New Proposal Button -->
        <a href="<%= charityproposal_path %>" class="new-proposal-btn">New Proposal</a>

        <!-- AI Allocation Engine Button -->
        <a href="<%= rationale_path %>" class="ai-allocation-btn">AI Allocation Engine</a>
    </section>

    <!-- Footer -->
    <footer>Â© 2025 1Cause | Empowering Change</footer>

</body>
</html>
