<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submit Proposal | 1Cause</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f4f7fa;
            color: #222;
            text-align: center;
            padding-bottom: 80px; /* Add padding to prevent content from being hidden behind the footer */
        }

        /* Navbar */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 50px;
            background: #0077B6;
            color: white;
        }

        .navbar .logo {
            font-size: 28px;
            font-weight: bold;
        }

        .navbar .login-btn {
            background: white;
            color: #0077B6;
            padding: 10px 20px;
            border-radius: 20px;
            text-decoration: none;
            font-weight: bold;
            transition: 0.3s ease-in-out;
        }

        .navbar .login-btn:hover {
            background: #005F99;
            color: white;
        }

        /* Hero Section */
        .hero {
            height: 60vh;
            background: linear-gradient(rgba(0, 158, 219, 0.8), rgba(0, 58, 100, 0.9)), url('https://source.unsplash.com/1600x900/?charity,helping');
            background-size: cover;
            background-position: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: white;
            padding: 20px;
        }

        .hero h1 {
            font-size: 50px;
            font-weight: 700;
            margin-bottom: 20px;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.2);
        }

        .hero p {
            font-size: 18px;
            max-width: 600px;
            margin-bottom: 30px;
        }

        /* Proposal Form Section */
        .form-container {
            background-color: white;
            padding: 40px;
            margin: -50px auto 50px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            max-width: 800px;
        }

        .form-container h2 {
            font-size: 36px;
            margin-bottom: 20px;
            text-align: left;
            color: #0077B6;
        }

        .form-container label {
            font-size: 16px;
            text-align: left;
            display: block;
            margin-bottom: 5px;
            color: #555;
        }

        .form-container input,
        .form-container textarea,
        .form-container select {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 14px;
        }

        .form-container textarea {
            resize: vertical;
            min-height: 100px;
        }

        .form-container button {
            background: #005F99;
            color: white;
            padding: 12px 25px;
            font-size: 18px;
            font-weight: bold;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            width: 100%;
        }

        .form-container button:hover {
            background: #004377;
        }

        /* Footer */
        footer {
            padding: 20px;
            background: #0077B6;
            color: white;
            font-size: 14px;
            position: fixed; /* Fix the footer to the bottom of the viewport */
            bottom: 0; /* Stick it to the bottom */
            left: 0; /* Align to the left edge */
            right: 0; /* Align to the right edge */
            width: 100%; /* Take up the full width */
            text-align: center; /* Center the text */
            z-index: 1000; /* Ensure it stays on top of other content */
        }
    </style>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
</head>
<body>

    <!-- Navbar -->
    <div class="navbar">
        <div class="logo">1Cause</div>
        <a href="<%= charitydashboard_path %>" class="login-btn">Back to Dashboard</a>
    </div>

    <!-- Hero Section -->
    <section class="hero">
        <h1>Submit Your Proposal</h1>
        <p>Tell us about your cause and the funding you need. We're here to help you make a real difference.</p>
    </section>

    <!-- Proposal Form Section -->
    <section class="form-container">
        <h2>Proposal Submission Form</h2>
        <form action="<%= submit_proposal_path %>" method="POST">
          <%= csrf_meta_tags %>
          
          <!-- Charity Name -->
          <label for="charity_name">Charity Name</label>
          <input type="text" id="charity_name" name="proposal[charity_name]" required>

          <!-- Contact Email -->
          <label for="contact_email">Contact Email</label>
          <input type="email" id="contact_email" name="proposal[contact_email]" required>

          <!-- Cause Title -->
          <label for="cause_title">Cause Title</label>
          <input type="text" id="cause_title" name="proposal[cause_title]" required>

          <!-- Cause Description -->
          <label for="cause_description">Cause Description</label>
          <textarea id="cause_description" name="proposal[cause_description]" required></textarea>

          <!-- Funding Needed -->
          <label for="funding_needed">Amount Needed (USD)</label>
          <input type="number" id="funding_needed" name="proposal[funding_needed]" required>

          <!-- Mission Statement -->
          <label for="mission_statement">Mission Statement</label>
          <textarea id="mission_statement" name="proposal[mission_statement]" required></textarea>

          <!-- Target Beneficiaries -->
          <label for="target_beneficiaries">Who Will Benefit?</label>
          <textarea id="target_beneficiaries" name="proposal[target_beneficiaries]" required></textarea>

          <!-- Project Timeline -->
          <label for="project_timeline">Project Timeline</label>
          <textarea id="project_timeline" name="proposal[project_timeline]" required></textarea>

          <!-- Expected Impact -->
          <label for="expected_impact">Expected Impact</label>
          <textarea id="expected_impact" name="proposal[expected_impact]" required></textarea>

          <!-- Corporate Matching -->
          <label for="corporate_matching">Corporate Matching Opportunity</label>
          <select id="corporate_matching" name="proposal[corporate_matching]" required>
              <option value="none">No corporate matching</option>
              <option value="yes">Yes, we have a matching sponsor</option>
          </select>

          <!-- Submit Button -->
          <button type="submit">Submit Proposal</button>
      </form>
    </section>

    <!-- Footer -->
    <footer>© 2025 1Cause | Empowering Change</footer>

</body>
</html>