<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>World Problems Dashboard</title>
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Poppins', sans-serif;
        }

        body {
        background-color: #f4f7fa;
        color: #222;
        text-align: center;
        padding: 30px;
        }

        h1 {
        font-size: 36px;
        font-weight: 700;
        margin-bottom: 20px;
        }

        .problems-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 20px;
        padding: 20px;
        }

        .card {
        background: white;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        width: 320px;
        padding: 20px;
        text-align: left;
        transition: 0.3s ease-in-out;
        display: flex;
        flex-direction: column;
        align-items: center;
        }

        .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }

        .card h2 {
        font-size: 22px;
        color: #0077B6;
        margin-bottom: 10px;
        text-align: center;
        }

        .card img {
        width: 100%;
        border-radius: 8px;
        margin-bottom: 15px;
        }

        .card p {
        font-size: 16px;
        color: #444;
        margin-bottom: 15px;
        text-align: center;
        }

        .card a, .card button {
        display: block;
        text-align: center;
        background: #0077B6;
        color: white;
        padding: 12px;
        font-size: 16px;
        text-decoration: none;
        border-radius: 5px;
        transition: 0.3s ease-in-out;
        border: none;
        cursor: pointer;
        width: 100%;
        margin-top: 10px;
        }

        .card a:hover, .card button:hover {
        background: #005F99;
        }

        .donate-btn {
        margin-top: 10px;
        width: 100%;
        }
    </style>
    </head>
    <body>
    <h1>Impending World Problems</h1>
    <div class="problems-container">
    <% @problems.each do |problem| %>
        <div class="card">
            <h2><%= problem[:name] %></h2>
            <img src="<%= problem[:image_url] %>" alt="<%= problem[:name] %>">
            <p><%= problem[:description] %></p>
            <a href="<%= problem[:link] %>" target="_blank">Read More</a>
            <form action="<%= payments_path(problem_name: problem[:name]) %>" method="get" style="width: 100%;">
                <button type="submit" class="donate-btn">Donate</button>
            </form>
            <p class="total-donated">Total Donated: <%= problem[:donated] %></p>
        </div>
    <% end %>
    </div>
    </body>
</html>